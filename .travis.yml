language: node_js
node_js:
  - '0.10'
  - '0.8'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-express-sequelize' ]; then cd .. && eval "mv $currentfolder generator-express-sequelize" && cd generator-express-sequelize; fi

